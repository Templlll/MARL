### Python 相关 ###
__pycache__/             # Python 字节码缓存，不需要版本控制
*.py[cod]                # 编译后的 Python 文件
*$py.class               # Java 编写的 Python 类文件
.pytest_cache/           # Pytest 测试缓存
.mypy_cache/             # Mypy 类型检查缓存

### 运行环境与依赖 ###
.venv/                   # 虚拟环境文件夹（重点：防止提交数 GB 的库文件）
venv/                    # 其他常见的虚拟环境命名
env/

### 训练日志与可视化 ###
logs/                    # 训练产生的日志文件
results/                 # 训练保存的结果数据（通常很大且是中间产物）
wandb/                   # Weights & Biases 的本地同步数据
*.log                    # 所有的日志文件

### 模型权重（强化学习重点） ###
*.pt                     # PyTorch 模型保存文件
*.pth                    # PyTorch 权重文件
*.model                  # 通用模型文件
*.ckpt                   # 检查点文件
# 如果您想保留某些特定的预训练模型，可以使用 !models/best_model.pt 手动包含

### 数据处理缓存 ###
*.npy                    # Numpy 二进制数据
*.npz                    # Numpy 压缩数据
*.h5                     # HDF5 数据文件
*.pkl                    # Pickle 序列化对象

### IDE 与操作系统 ###
.vscode/                 # VS Code 的项目配置（通常包含个人路径，不建议提交）
.idea/                   # JetBrains (PyCharm) 的配置
.DS_Store                # macOS 产生的索引文件
Thumbs.db                # Windows 产生的缩略图缓存